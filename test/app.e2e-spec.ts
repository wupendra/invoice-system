// Placeholder — replaced by test/e2e/auth.e2e-spec.ts
// The auto-generated "Hello World!" test is no longer valid once auth guards are wired up.
describe('AppController (e2e)', () => {
  it('placeholder — see test/e2e/auth.e2e-spec.ts for real coverage', () => {
    expect(true).toBe(true);
  });
});
